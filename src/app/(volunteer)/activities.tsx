// The existing volunteer navigator exposes this as /activities. Keeping the route lets
// the Activity History screen appear in that navigation without adding a second menu.
export { default } from '../volunteer-activity-history';
