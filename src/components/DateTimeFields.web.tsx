import { StyleSheet, Text, TextInput, View } from 'react-native';
import { colors } from '../theme/colors';
export function DateTimeFields({ date, time, onDateChange, onTimeChange }: { date: string; time: string; onDateChange: (value: string) => void; onTimeChange: (value: string) => void }) { return <View style={styles.row}><View style={styles.column}><Text style={styles.label}>Date</Text><TextInput accessibilityLabel="Date" value={date} onChangeText={onDateChange} placeholder="YYYY-MM-DD" placeholderTextColor={colors.inputPlaceholder} style={styles.input} /></View><View style={styles.column}><Text style={styles.label}>Time</Text><TextInput accessibilityLabel="Time" value={time} onChangeText={onTimeChange} placeholder="14:30" placeholderTextColor={colors.inputPlaceholder} style={styles.input} /></View></View>; }
const styles = StyleSheet.create({ row: { flexDirection: 'row', gap: 11, marginBottom: 22 }, column: { flex: 1 }, label: { color: colors.textPrimary, fontSize: 17, fontWeight: '800', marginBottom: 8 }, input: { minHeight: 58, borderRadius: 15, borderWidth: 1, borderColor: colors.inputBorder, backgroundColor: colors.surface, paddingHorizontal: 14, color: colors.textPrimary, fontSize: 16 } });
